version: '2'
services:
  web:
    build: .
    volumes:
      - ./app:/app
    depends_on:
      - mongodb
    ports:
      - "5000:5000"
    command: python3 /app/app.py
  mongodb:
    image: mongo:3.4
    volumes:
      - ./data:/data/db
